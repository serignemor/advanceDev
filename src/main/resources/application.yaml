spring:
  datasource:
    url: jdbc:mysql://localhost:8889/advancedev
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Hibernate settings
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        type: trace